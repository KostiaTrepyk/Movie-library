import Router from "./Router/Router";
import Theme from "./Theme/Theme";

const InitApp = () => {
    return (
        <Theme>
            <Router />
        </Theme>
    );
};

export default InitApp;
